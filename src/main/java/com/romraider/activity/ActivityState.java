/* RomRaider2 ECU Studio - GPL 2.0 or later. */
package com.romraider.activity;

/** Lifecycle state for a user-visible application operation. */
public enum ActivityState {
    IDLE, RUNNING, SUCCEEDED, FAILED
}
